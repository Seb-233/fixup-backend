apiVersion: v1
kind: Service
metadata:
  name: fixup-backend
  namespace: fixup
  labels:
    app: fixup-backend
    app.kubernetes.io/part-of: fixup
spec:
  selector:
    app: fixup-backend
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: 8080
